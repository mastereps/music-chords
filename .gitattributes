# Shell scripts must keep LF endings. Checked out with CRLF on Windows they reach the Linux VPS
# as `#!/usr/bin/env bash\r`, which fails with a "bad interpreter" error that reads like a missing file.
*.sh text eol=lf
