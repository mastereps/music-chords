/**
 * Shared column template so the table header and every item row stay aligned.
 * Below `md` the row stacks into a card and the header is hidden.
 */
export const ROW_GRID = 'grid grid-cols-1 md:grid-cols-[minmax(0,1.6fr)_132px_128px_minmax(0,1.5fr)_116px_40px]';
